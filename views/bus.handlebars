<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Rutgers Bus Routes</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 20px; }
    h1 { color: #c8102e; }
    .bus-route { margin-bottom: 20px; }
    .bus-name { font-weight: bold; font-size: 1.2em; margin-bottom: 5px; }
    ul { margin: 0; padding-left: 20px; }
  </style>
</head>
<body>
  <h1>Rutgers Buses</h1>

  {{#if buses.length}}
    {{#each buses}}
      <div class="bus-route">
        <div class="bus-name">{{this.name}}</div>
        <ul>
          {{#each this.stops}}
            <li>{{this}}</li>
          {{/each}}
        </ul>
      </div>
    {{/each}}
  {{else}}
    <p>No bus routes available.</p>
  {{/if}}

</body>
</html>
